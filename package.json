{
  "name": "sjis-web-example-builder",
  "version": "1.0.0",
  "description": "Build scripts for the Shift-JIS web example.",
  "private": true,
  "scripts": {
    "build": "node build/build.js",
    "start": "npm run build && python3 server/api_server.py"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "ejs": "^3.1.10",
    "fs-extra": "^11.2.0",
    "iconv-lite": "^0.6.3"
  }
}
